info:
  name: List-Units
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8082/api/unit/list-units
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: ""

runtime:
  variables:
    - name: ""
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
