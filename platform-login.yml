info:
  name: platform-login
  type: http
  seq: 2

http:
  method: POST
  url: "{{local_env}}/auth/platform-login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |+
      {
          "email":"jenish1@gmail.com",
          "password": "jenish@123"
        
      }



settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
