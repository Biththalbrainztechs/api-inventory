info:
  name: platform-login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8082/api/auth/platform-login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "bits@gmail.com",
          "password": "secret123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
